services:
  # Backing Database for IoC Knowledge Memory Service
  # Graph(Agensgraph) + PgVector + Postgres Database
  # Port Mapping: 5456:5432
  #   - Host Port: 5456 (to avoid conflicts with local PostgreSQL on 5432)
  #   - Container Port: 5432 (PostgreSQL default, cannot be changed without config)
  #   - it will have cfn_mgmt_db and ioc_knowledge_db both
  ioc-knowledge-db:
    image: ghcr.io/outshift-open/ioc-knowledge-memory-svc-db:0.2.1
    platform: ${DOCKER_DEFAULT_PLATFORM:-linux/arm64}
    container_name: ioc-knowledge-db
    profiles:
      - db-only
      - full-stack
    environment:
      POSTGRES_DB: ${IOC_KNOWLEDGE_DB:-ioc-knowledge-db}
      POSTGRES_USER: ${IOC_KNOWLEDGE_DB_USER}
      POSTGRES_PASSWORD: ${IOC_KNOWLEDGE_DB_PASSWORD}
    ports:
      - "${IOC_KNOWLEDGE_DB_PORT:-5456}:5432"
    volumes:
      - ioc_knowledge_db_data:/var/lib/postgresql/data
      - ./scripts/init-multi-db.sh:/docker-entrypoint-initdb.d/init-multi-db.sh:ro
    restart: unless-stopped
    healthcheck:
      test: [
          "CMD-SHELL",
          "pg_isready -h localhost -p 5432 -U ${IOC_KNOWLEDGE_DB_USER} -d
          ${IOC_KNOWLEDGE_DB:-ioc-knowledge-db}",
        ]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 30s

  # Graph Database Viewer - AgensGraph UI
  # Port Mapping: 5457:3000
  # Uncomment this section to access the AgensGraph UI
#  ioc-graph-db-viewer:
#    image: skaiworldwide/agviewer:latest
#    platform: ${DOCKER_DEFAULT_PLATFORM:-linux/arm64}
#    container_name: ioc-graph-db-viewer
#    profiles:
#      - db-only
#      - full-stack
#    ports:
#      - "5457:3000"
#    environment:
#      - DB_HOST=ioc-knowledge-db
#      - DB_PORT=5432
#      - DB_USER=${IOC_KNOWLEDGE_DB_USER}
#      - DB_PASSWORD=${IOC_KNOWLEDGE_DB_PASSWORD}
#      - DB_NAME=${IOC_KNOWLEDGE_DB:-ioc-knowledge-db}
#    restart: unless-stopped
#    depends_on:
#      - ioc-knowledge-db
  ### End Database Services ###

  ### Application Services ###
  # Port Mapping Format: "HOST_PORT:CONTAINER_PORT"
  # - HOST_PORT: The port on your local machine (what you connect to)
  # - CONTAINER_PORT: The port the service listens on inside the container

  # Mgmt. Plane Backend Service - FastAPI backend for workspaces, users, CFN nodes
  # Port Mapping: 9000:9000 (same port inside and outside container)
  ioc-cfn-mgmt-plane-svc:
    build:
      context: .
      dockerfile: Dockerfile
    # Image Link: https://github.com/orgs/outshift-open/packages/container/package/ioc-cfn-mgmt-plane-svc
    image: ghcr.io/outshift-open/ioc-cfn-mgmt-plane-svc:v0.2.0
    platform: ${DOCKER_DEFAULT_PLATFORM:-linux/arm64}
    container_name: ioc-cfn-mgmt-plane-svc
    profiles:
      - full-stack
    ports:
      - "9000:9000"
    environment:
      - SERVICE_NAME=ioc-cfn-mgmt-plane-svc
      - APPLICATION_VERSION=dev
      - LOG_LEVEL=${LOG_LEVEL:-INFO}
      - ADMIN_USER_PASSWORD=${ADMIN_USER_PASSWORD}
      - POSTGRES_HOST=ioc-knowledge-db
      - POSTGRES_PORT=5432
      - POSTGRES_DB=cfn_mgmt
      - POSTGRES_USER=${IOC_KNOWLEDGE_DB_USER}
      - POSTGRES_PASSWORD=${IOC_KNOWLEDGE_DB_PASSWORD}
      - PORT=9000
      - CFN_DEV_MODE=${CFN_DEV_MODE}
      - CFN_SVC_URL=${CFN_SVC_URL:-http://ioc-cfn-svc:9002}
    volumes:
      - ./.secrets:/home/app/.secrets:ro
    restart: unless-stopped
    depends_on:
      ioc-knowledge-db:
        condition: service_healthy
    healthcheck:
      test:
        [
          "CMD",
          "curl",
          "-f",
          "http://ioc-cfn-mgmt-plane-svc:9000/api/internal/diagnostics/health",
        ]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s

  # Mgmt. Plane UI - Next.js frontend for management plane
  # Port Mapping: 9001:9001 (same port inside and outside container)
  ioc-cfn-mgmt-plane-ui:
    # Image Link: https://github.com/orgs/outshift-open/packages/container/package/ioc-cfn-mgmt-plane-ui
    image: ghcr.io/outshift-open/ioc-cfn-mgmt-plane-ui:v0.2.0
    platform: ${DOCKER_DEFAULT_PLATFORM:-linux/arm64}
    container_name: ioc-cfn-mgmt-plane-ui
    profiles:
      - full-stack
    ports:
      - "9001:9001"
    environment:
      - NODE_ENV=production
      - PORT=9001
      - CFN_UI_API_BASE_URL=http://ioc-cfn-mgmt-plane-svc:9000
      - NEXT_PUBLIC_CFN_UI_API_BASE_URL=http://localhost:9000
      - NEXTAUTH_URL=${NEXTAUTH_URL:-http://localhost:9001}
      - NEXTAUTH_SECRET=${NEXTAUTH_SECRET}
    restart: unless-stopped
    depends_on:
      ioc-cfn-mgmt-plane-svc:
        condition: service_healthy

  # Knowledge Memory Service - Graph + Vector Memory Storage
  # Port Mapping: 9003:9003 (same port inside and outside container)
  ioc-knowledge-memory-svc:
    # Image Link: https://github.com/orgs/outshift-open/packages/container/package/ioc-knowledge-memory-svc
    image: ghcr.io/outshift-open/ioc-knowledge-memory-svc:v0.2.4
    platform: ${DOCKER_DEFAULT_PLATFORM:-linux/arm64}
    container_name: ioc-knowledge-memory-svc
    profiles:
      - full-stack
    ports:
      - "9003:9003"
    environment:
      - SERVICE_NAME=ioc-knowledge-memory-svc
      - APPLICATION_VERSION=dev
      - LOG_LEVEL=${LOG_LEVEL:-INFO}
      - MEMORY_PROVIDER_REGISTRATION_URL=http://ioc-cfn-mgmt-plane-svc:9000
      - MEMORY_PROVIDER_ID=${MEMORY_PROVIDER_ID:-de54dacb-60ab-476a-9e83-64e09f867358}
      - IOC_KNOWLEDGE_DB_HOST=ioc-knowledge-db
      - IOC_KNOWLEDGE_DB=${IOC_KNOWLEDGE_DB:-ioc-knowledge-db}
      - IOC_KNOWLEDGE_DB_USER=${IOC_KNOWLEDGE_DB_USER}
      - IOC_KNOWLEDGE_DB_PASSWORD=${IOC_KNOWLEDGE_DB_PASSWORD}
      - IOC_KNOWLEDGE_DB_PORT=5432
      - EMBEDDING_VECTOR_SIZE=${EMBEDDING_VECTOR_SIZE:-384}
    restart: unless-stopped
    depends_on:
      ioc-knowledge-db:
        condition: service_healthy
      ioc-cfn-mgmt-plane-svc:
        condition: service_healthy
    healthcheck:
      test:
        [
          "CMD",
          "curl",
          "-f",
          "http://localhost:9003/api/internal/diagnostics/health",
        ]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s

  # CFN Service - Cognition Fabric Node service
  # Port Mapping(HTTP): 9002:9002 (same port inside and outside container)
  # Port Mapping(MCP): 9009:9001 (different port inside and outside container)
  ioc-cfn-svc:
    # Image Link: https://github.com/orgs/outshift-open/packages/container/package/ioc-cfn-svc
    image: ghcr.io/outshift-open/ioc-cfn-svc:v0.2.0
    platform: ${DOCKER_DEFAULT_PLATFORM:-linux/arm64}
    container_name: ioc-cfn-svc
    profiles:
      - full-stack
    restart: unless-stopped
    ports:
      - "9002:9002"
      - "9009:9001"
    environment:
      - LOG_LEVEL=${LOG_LEVEL:-INFO}
      - MGMT_URL=http://ioc-cfn-mgmt-plane-svc:9000
      - CFN_NAME=${CFN_NAME:-cfn-new}
      - HEARTBEAT_INTERVAL_SECONDS=${HEARTBEAT_INTERVAL_SECONDS:-10}
      - SERVICE_NAME=${SERVICE_NAME:-ioc-cfn-svc}
      - DB_NAME=${DB_NAME:-cfn_cp}
      - DB_USER=${POSTGRES_USER}
      - DB_PASSWORD=${POSTGRES_PASSWORD}
      - DB_HOST=ioc-knowledge-db
      - DB_PORT=5432
      - KNOWLEDGE_MEMORY_SVC_URL=http://ioc-knowledge-memory-svc:9003
      - LLM_PROVIDER=${LLM_PROVIDER:-azure-openai}
      - COGNITION_ENGINE_SVC_URL=http://ioc-cfn-cognition-engine:9004
      - MCP_PORT=9001
    depends_on:
      ioc-knowledge-db:
        condition: service_healthy
      ioc-cfn-mgmt-plane-svc:
        condition: service_healthy
      ioc-knowledge-memory-svc:
        condition: service_healthy
    healthcheck:
      test:
        [
          "CMD",
          "curl",
          "-f",
          "http://localhost:9002/api/internal/diagnostics/health",
        ]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s

  # Cognition Engines
  # Port Mapping: 9004:9004 (same port inside and outside container)
  ioc-cfn-cognition-engine:
    # Image Link: https://github.com/orgs/outshift-open/packages/container/package/ioc-cfn-cognition-engines
    image: ghcr.io/outshift-open/ioc-cfn-cognition-engines:v0.2.0
    platform: ${DOCKER_DEFAULT_PLATFORM:-linux/arm64}
    container_name: ioc-cfn-cognition-engine
    ports:
      - "9004:9004"
    profiles:
      - full-stack
    restart: unless-stopped
    environment:
      - LLM_BASE_URL=${LLM_BASE_URL}
      - LLM_API_KEY=${LLM_API_KEY}
      - LLM_MODEL=${LLM_MODEL}
      - ENABLE_DEDUP=true
      # CE lifecycle management - auto-registration and heartbeat
      - CFN_URL=http://ioc-cfn-svc:9002
      - COGNITION_ENGINE_HOST=ioc-cfn-cognition-engine
      - COGNITION_ENGINE_PORT=9004
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:9004/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s
    depends_on:
      ioc-cfn-mgmt-plane-svc:
        condition: service_healthy
      ioc-cfn-svc:
        condition: service_healthy

volumes:
  ioc_knowledge_db_data:
